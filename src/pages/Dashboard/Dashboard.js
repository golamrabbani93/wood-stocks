import React from 'react';
import UseTitle from '../../hooks/UseTitle';

const Dashboard = () => {
	UseTitle('Dashboard | Woods Stoks');
	return (
		<div className="text-left">
			<h2>this is Dashboard</h2>
		</div>
	);
};

export default Dashboard;
